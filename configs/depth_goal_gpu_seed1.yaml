exp_name: depth_goal_gpu_seed1

seed: 1
  
env_id: PushCubeDepthGoal-v1
obs_mode: depth
encoder_type: depth

sim_backend: physx_cuda
cuda: true

num_envs: 2048
num_eval_envs: 16

num_steps: 20
total_timesteps: 2000000

num_minibatches: 32
update_epochs: 8

eval_freq: 10
num_eval_steps: 200
